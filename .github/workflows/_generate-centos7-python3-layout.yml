name: 'Generate CentOS 7 Python Layout Patch'

on:
  push:
    branches:
      - main
    paths:
      - '.github/workflows/_generate-centos7-python3-layout.yml'

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 1
          sparse-checkout: |
            .github

      - name: 'Generate patched workflow payloads'
        run: |
          set -euxo pipefail
          python3 .github/patch-centos7-python3-layout.py
          mkdir -p .github/generated
          cp .github/workflows/centos7-verify.yml .github/generated/centos7-verify-layout.yml.txt
          cp .github/workflows/centos7-release.yml .github/generated/centos7-release-layout.yml.txt
          git restore .github/workflows/centos7-verify.yml .github/workflows/centos7-release.yml
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .github/generated/centos7-verify-layout.yml.txt .github/generated/centos7-release-layout.yml.txt
          git diff --cached --check
          git commit -m 'ci: generate current manylinux Python layout patch payloads'
          git push origin HEAD:main
